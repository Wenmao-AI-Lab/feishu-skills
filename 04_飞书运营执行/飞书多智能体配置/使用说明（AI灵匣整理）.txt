【所属分类】飞书 · 飞书运营执行
【技能名称】飞书多智能体配置
【技能标识】feishu-multi-agent
【功能描述】|
【详细概述】
# 飞书多智能体配置
**一句话定位：**|
> name: feishu-multi-agent description: | 配置多个 OpenClaw agent 绑定多个飞书（Lark）机器人，在同一个群聊中实现多智能体协作。 当用户要求：(1) 在飞书中部署多个 AI 智能体；(2) 多个飞书机器人共享群聊； (3) 飞书群聊中实现 bot 间协作或互操作；(4) 配置 openclaw-lark 多账号时使用。

━━━━━━━━━━━━━━━━━━━━ 淘宝店AI灵匣 ━━━━━━━━━━━━━━━━━━━━

# 飞书多智能体配置指南

将多个 OpenClaw agent 与多个飞书机器人 App 绑定，实现在同一群聊中的多智能体协作。

━━━━━━━━━━━━━━━━━━━━ 淘宝店AI灵匣 ━━━━━━━━━━━━━━━━━━━━

## 前置条件

1. 每个 agent 需要一个独立的飞书自建应用（AppID + AppSecret）
2. 每个飞书应用需要开启：机器人能力、WebSocket 长连接
3. 每个飞书应用需要添加到目标群聊中
4. 已安装 `openclaw-lark` 插件

━━━━━━━━━━━━━━━━━━━━ 淘宝店AI灵匣 ━━━━━━━━━━━━━━━━━━━━

### 1. 飞书开放平台创建应用

每个 agent 对应一个飞书应用，共需 N 个应用。

每个应用需要：
- 开启「机器人」能力
- 权限范围：`im:message`、`im:message:send_as_bot`、`im:chat:readonly` 等
- 事件订阅：`im.message.receive_v1`（通过 WebSocket 模式，无需公网回调）
- 记录 AppID 和 AppSecret

━━━━━━━━━━━━━━━━━━━━ 淘宝店AI灵匣 ━━━━━━━━━━━━━━━━━━━━

### 2. OpenClaw 配置

配置涉及 4 个关键部分：

- `agents.list` — 定义每个 agent
- `channels.feishu.accounts` — 配置每个飞书应用凭据
- `bindings` — 将飞书账号绑定到 agent
- `channels.feishu.accounts.default` — 访问控制策略

详细配置模板见 [references/config-template.md](references/config-template.md)

━━━━━━━━━━━━━━━━━━━━ 淘宝店AI灵匣 ━━━━━━━━━━━━━━━━━━━━

### 3. 重启验证

```bash
openclaw gateway restart
openclaw doctor
```

━━━━━━━━━━━━━━━━━━━━ 淘宝店AI灵匣 ━━━━━━━━━━━━━━━━━━━━

## 智能体间协作

由于飞书 WebSocket 不会将其他机器人的消息触发为事件，agent 间通信使用 OpenClaw 内部机制：

- **`sessions_send(agentId: "xxx", message: "...")`** — 向另一个 agent 发送消息并等待回复
- **`sessions_list`** — 查看活跃 session
- **`sessions_spawn`** — 启动子 agent 处理子任务

在 agent 的 SOUL.md 或 AGENTS.md 中编写协作规则，定义何时调用另一个 agent。

━━━━━━━━━━━━━━━━━━━━ 淘宝店AI灵匣 ━━━━━━━━━━━━━━━━━━━━

## 常见问题

详见 [references/troubleshooting.md](references/troubleshooting.md)

【安装方式】（三步安装 · 复制文件夹即用）
  只需三步，即可把本技能装进AI对话框直接使用：

  第1步｜复制技能文件夹：找到本技能所在的文件夹，
       右键 → 复制路径（或直接复制整个文件夹）。

  第2步｜放入智能体对话框：把复制好的文件夹路径粘贴到对话框里
       （也可以直接把文件夹拖进对话框输入框，和发图片一样）。

  第3步｜输入安装指令，把下面这句话粘贴发送：

  【请解压这个技能包「飞书多智能体配置」，并安装到技能目录，
  完成后告诉我，后续对话我会直接使用它。】

  · 等AI回复"安装完成"即可正常使用。
  · 多个技能一起安装：把多个文件夹路径/文件夹一起放入对话框，
    再发送："请把上面这些技能包全部解压安装"。
  · 安装后直接描述需求，AI会自动调用本技能。

【使用说明】
  · 本技能包由「淘宝店：AI灵匣」整理提供，安装后即可通过自然语言触发。
  · 具体功能以技能包内 SKILL.md 为准，如有问题可重新安装。
